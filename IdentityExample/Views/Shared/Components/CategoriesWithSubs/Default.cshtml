@model (List<Category> Categories, string CurrentCategory)
@using IdentityExample.Models

<ul style="list-style-type:none; padding: 0">
    @foreach (Category category in Model.Categories.Where(c=>c.ParentCategoryId == null))
    {
    <li>
        @*<a asp-action="GetCategories" asp-controller="Categories"*@
        <a asp-action="GetCategories" asp-controller="Categories" asp-route-title="@category.Title">
            @category.Title
        </a>
        @*@if (category.ChildCategories.Any())
        {
            <vc:categories-with-subs parent-id="@category.Id"
                                     current-category="@Model.CurrentCategory">
            </vc:categories-with-subs>
        }*@
    </li>
        }
</ul>

        @*class="@(category==Model.Item2 ? "btn btn-outline-primary w-100": "btn btn-outline-secondary w-100")"
        </div>*@
@*</ul>*@